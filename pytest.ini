[pytest]
python_files = *.py
norecursedirs = .* _* node node_modules coverage venv
addopts =
    --tb short
    --cov=metadeploy
    --cov-report html
    --cov-report term
    --cov-report xml
    --cov-fail-under 100
    --ds=config.settings.test
