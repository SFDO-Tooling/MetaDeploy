[pytest]
python_files = *.py
norecursedirs = .* _* node node_modules coverage venv
addopts =
    --tb short
    --cov=metadeploy
    --cov-report html
    --cov-report term
    --ds=config.settings.test
